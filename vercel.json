{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf public && mkdir -p public && cp -r index.html assets design-system ds.html public/ && cd quiz-app && npm install --no-audit --no-fund && npm run build",
  "outputDirectory": "public",
  "framework": null,
  "cleanUrls": true
}
